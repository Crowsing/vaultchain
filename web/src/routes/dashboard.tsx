/**
 * Dashboard placeholder — `phase1-web-004` replaces this with the
 * real content per AC-phase1-web-005-08.
 */
export function DashboardRoute(): React.JSX.Element {
  return (
    <div
      className="rounded-lg bg-bg-surface p-8 text-center"
      data-testid="dashboard-placeholder"
    >
      <h2 className="text-lg font-semibold text-text-primary">Dashboard</h2>
      <p className="mt-2 text-sm text-text-secondary">
        Coming in next brief — the real dashboard lands in phase1-web-004.
      </p>
    </div>
  );
}
