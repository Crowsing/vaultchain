// PHASE 1 STUB — replaced in Phase 2 by useTransactionsQuery() from chains.

export type StubTransaction = never;

export function getStubTransactions(): ReadonlyArray<StubTransaction> {
  return [];
}
